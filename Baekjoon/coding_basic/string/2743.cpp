#include <iostream>
using namespace std;

int main()
{
    // freopen("input.txt","r",stdin);
    string s;
    int count = 0;
    cin >> s;
    cout << s.size() << endl; // O(1)
    // cout << s.length() << endl; // O(n)
    return 0;
}